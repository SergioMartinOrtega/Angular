import { ViewsDirective } from './views.directive';

describe('ViewsDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewsDirective();
    expect(directive).toBeTruthy();
  });
});
